{
  "variables": [
    {
      "name": "PORT",
      "description": "HTTP port for the LaBass Technologies web server. Defaults to the dedicated port 18080.",
      "example": "18080",
      "required": false
    }
  ]
}
